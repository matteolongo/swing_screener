# Frontend Code Review Plan

Index for the `web-ui/` deep review handoff.

- `plan.md` — executable review plan with scope, work items, verification, and findings format.

Use `plan.md` with the local repository checkout at `/workspace/swing_screener`. The review is findings-only: do not edit application code, tests, configs, package files, snapshots, generated output, or docs as part of the review execution unless a separate follow-up task explicitly asks for fixes.
