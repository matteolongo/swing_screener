/**
 * Custom React hooks for the Swing Screener application.
 * 
 * These hooks provide reusable state management patterns and eliminate
 * duplication across components.
 */

export { useLocalStorage } from './useLocalStorage';
export { useModal, type UseModalReturn } from './useModal';
